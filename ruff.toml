line-length = 88
target-version = "py312"

extend-exclude = [
  "backend/**/migrations/*",
  "backend/.venv",
  "frontend",
]

[lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[lint.isort]
known-first-party = ["backend", "config", "shop"]
